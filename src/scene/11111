t config --global user.email "tanglongfeifei@163.com"
git config --global user.name "tanglong"
git add -A
git commit -m 'first commit'
git push origin master


git checkout .


git使用免秘钥的方式登录一定要使用ssh克隆仓库，否则是明文保存密码了
mkdir ~/.ssh
cd .ssh/
ssh-keygen -t rsa -C "tanglongfeifei@163.com"
sz id_rsa.pub  将里面的内容设置到github的设置页面

git clone git@github.com:tanglong/go_heap.git 初始化仓库即可    

